import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'office-space-manager',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
